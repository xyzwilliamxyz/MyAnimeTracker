<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/anime_group" android:checkableBehavior="single" >
        <item android:id="@+id/nav_tracking"
              android:icon="@drawable/ic_home"
              android:title="@string/tracking"/>

        <item android:id="@+id/nav_seasonal"
              android:icon="@drawable/ic_seasonal"
              android:title="@string/seasonal_anime"/>
    </group>

    <group android:id="@+id/settings_group" android:checkableBehavior="single" >
        <item android:id="@+id/nav_settings"
              android:icon="@drawable/ic_settings"
              android:title="@string/settings"/>
    </group>

</menu>
